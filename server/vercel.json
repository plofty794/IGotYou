{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",

      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",

      "dest": "server.ts"
    }
  ],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
